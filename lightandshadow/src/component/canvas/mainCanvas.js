import React, {useRef, useEffect } from 'react'
import styles from './mainCanvas.scss'

const MainCanvas = (props) =>{
    return (
        <div className="mainCanv">
            <div>
                        ss
            </div>
        </div> 
        )}

export default MainCanvas